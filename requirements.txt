fastapi==0.110.0         # 웹 서버를 만드는 데 사용되는 프레임워크
uvicorn[standard]==0.29.0 # FastAPI 서버를 실행하는 데 사용되는 서버 프로그램
aiortc==1.7.0            # WebRTC 데이터 채널 통신을 위한 라이브러리
python-multipart==0.0.9  # 파일 업로드 등 멀티파트 폼 데이터를 처리하기 위해 필요

# === 머신러닝 및 영상 처리 라이브러리 ===
# 아래 라이브러리들은 설치에 시간이 오래 걸릴 수 있습니다.
torch==2.2.0             # 모델 추론을 위한 파이토치 라이브러리
numpy==1.26.4            # 숫자 및 행렬 연산을 위한 필수 라이브러리
mediapipe==0.10.11       # 영상 프레임에서 손/몸의 랜드마크를 추출하는 라이브러리
opencv-python==4.9.0.80   # 클라이언트가 보낸 이미지 프레임을 처리(디코딩)하는 라이브러리
Pillow==10.3.0           # 이미지 처리를 위한 보조 라이브러리

# === 테스트용 라이브러리 ===
httpx==0.27.0            # smoke_test.py 스크립트 실행에 필요
